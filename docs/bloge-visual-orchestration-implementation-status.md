# BLOGE 可视化编排实现状态审计

状态：Current Implementation Snapshot
日期：2026-07-02
代码基线：`resource-gateway-examples`

本文不是新的愿景文档，而是把当前代码事实、已完成闭环、仍缺口和下一步工程重点对齐起来。它用于防止设计文档、README 和实际实现发生语义漂移。

## 1. 当前结论

Phase 1 的核心闭环已经从“设计草案”进入“可运行示例项目”：

```text
Operator Library / Resource Design Contract
        -> Visual Operator Catalog
        -> GraphDraft
        -> Schema-aware Connection / Validation
        -> BLOGE DSL Generation
        -> Compile / Run
        -> Immutable Publication
```

现在最重要的判断不是“有没有画布雏形”，而是：这个示例已经具备通用画布的控制面骨架，但仍不是完整低代码平台。它可以严肃演示用户算子库、Java OperatorRegistry 投影、资源虚拟算子、OpenAPI 辅助生成 resource design contract、schema 约束连线、草稿、发布和运行；并且已经能在 catalog/validator 层识别并阻断当前 request-response runtime 不能执行的 streaming/durable operator；还没有覆盖 durable 实例、远程 worker/subgraph marketplace、生产 IAM 后台和多人协作。

## 2. 已落地能力

| 能力域 | 当前状态 | 代码证据 |
| --- | --- | --- |
| 用户算子库导入 | 已落地。支持 `bloge.visualOperatorLibrary.v1`、生命周期、policy、schema/lowering/DSL 可寻址性校验、`oneOf`/`anyOf` visual union schema、H2 持久化、create/replace/delete/restore 不可变 revision history、跨库 `operatorRef` 归属保护，以及面向 stored draft 和 immutable publication 的 replacement/delete impact preflight；`DEPRECATED` library operator 默认从 palette 隐藏，但为 stored draft / includeDeprecated catalog resolution 保留，并投影 `visual.operator.lifecycle.deprecated` warning，草稿校验会把它归因到具体节点，发布时复用 `ackWarnings` gate，避免弃用算子静默进入生产 promotion；当库从非 deprecated 状态降级为 `DEPRECATED` 且已有草稿或发布物引用时，validate/import/replace 会返回 `visual.library.lifecycle.deprecated` 或 `visual.library.publicationLifecycleDeprecated` warning，并要求 `ackWarnings=true` 后才会存储；导入边界会在 executable lowering mode 下拦截 DSL-unsafe input port、input schema 和 output schema 字段，native lowering 还会校验 config schema；`lowering.mode=design` 已作为 schema-only authoring 模式落地，允许尚无 runtime 实现的用户算子进入 catalog、拖拽编排、schema 校验、保存、导出和浏览器可选 `artifactKind=DESIGN` 的非执行型 publication 冻结，但 compile/run/default executable publish 会以 `visual.codegen.designOnlyOperator` 明确阻断；native lowering 会在 runtime Java inventory 可见时预警 unresolved executable `operatorRef`，并复用 `ackWarnings` gate，避免未审阅的不可执行 wrapper 静默进入 catalog；import-time capability/governance preflight 会对 streaming/durable runtime requirement、secret-backed execution 和 `NON_IDEMPOTENT` 外部副作用给出 warning，并要求 `ackWarnings=true` 后才可入库；same-ref fingerprint drift warning 会带 changed-surface 摘要和 `metadata.changeRisk/changeCategories`，把 replacement 变更归类为 `BREAKING_SCHEMA`、`COMPATIBLE_SCHEMA`、`RUNTIME_BINDING`、`GOVERNANCE`、`POLICY` 或 `METADATA`，说明 input/output/config schema、capability、policy 或 lowering 哪个面发生变化以及升级风险；replacement SemVer 治理已进入同一校验面，版本回退是 blocking error，breaking schema / operator removal / disablement 未提升 major version 会 warning-gate，additive 或 compatible schema 变更未提升 minor version 也会 warning-gate，且 diagnostics metadata 会携带 affected operatorRefs 和 change-risk 信息。validate、warning-gated import/replace 和 delete conflict 响应返回 `bloge.visualOperatorLibraryImpact.v1`，浏览器导入面板优先消费该合同展示 affected draft/publication/operator/code、change risk counts、affected draft node target 和 affected publication node target 摘要，并可从 affected draft chip 直接加载草稿、聚焦受影响节点，也可从 affected publication chip 选中发布物进入复核；算子库 revision API 可在误删、坏版本导入或 schema drift 争议后查询历史快照并受治理恢复为新的 latest revision，restore 记录 `RESTORE` 和来源 revision，复用 replacement preflight，版本回退默认阻断且只有 `allowVersionRegression=true&ackWarnings=true` 才可受控 rollback；delete 不会清除审计历史；validate、warning-gated import/replace 和 delete conflict 响应还返回服务端派生的 `bloge.visualOperatorLibraryProfile.v1`，暴露 DSL-unsafe 字段/端口、dynamic schema、design-only authoring、runtime readiness facet、catalog-repair、runtime-blocked、governance-review、streaming/durable runtime requirement、external effect、non-idempotent side effect、secret-bound operators 和 scope-restricted policy 摘要。浏览器导入前 profile 优先消费该服务端 profile，只有用户继续编辑 JSON 或服务端 profile 缺失时才回退到本地即时预览。 | `visual/catalog/OperatorLibrary.java`、`OperatorLibraryRevision.java`、`OperatorLibraryValidator.java`、`OperatorLibraryAdminController.java`、`OperatorLibraryImpactReview.java`、`OperatorLibraryProfile.java`、`OperatorLibraryValidationResult.java`、`OperatorDefinitionChangeSummary.java`、`DatabaseOperatorLibraryRegistry.java`、`static/examples/gateway/app.js` |
| 视觉算子定义 | 已落地。`OperatorDefinition` 统一表达 operatorRef、fingerprint、display、source、ports、configSchema、capabilities（effect、idempotency、streaming、durable、requiresSecrets）、policy、lowering、diagnostics 和服务端派生的 `runtimeReadiness`；`runtimeReadiness` 把当前 request-response visual runtime 下的 `RUNTIME_EXECUTABLE`、`DESIGN_ONLY`、`RUNTIME_BLOCKED`、`GOVERNANCE_REVIEW`、`CATALOG_REPAIR_REQUIRED` 状态作为 wire contract 暴露，而不是让浏览器自行猜测。 | `visual/catalog/OperatorDefinition.java` |
| Resource 虚拟算子 | 已落地。`ResourceDescriptor + ResourceDesignContract` 投影为 `resource:<resourceId>`，运行时 lowering 到 `httpResource`。 | `visual/catalog/ResourceVirtualOperatorProjector.java`、`DefaultVisualOperatorCatalog.java` |
| Java OperatorRegistry 投影 | 已落地基础版。Spring `OperatorRegistry` 中的 Java `Operator` / `StreamingOperator` / `SuspendableOperator` 可投影为 visual operator，复用 BLOGE schema metadata、注解描述、capabilities 和 native lowering；catalog 会区分普通、streaming、suspendable Java source kind，`SuspendableOperator` 会标记 `capabilities.durable=true`，运行时把 DSL map 输入适配到 Java DTO/record，并支持 record 输出路径选择；BLOGE `UnionSchema` 会投影为 visual `oneOf` schema，继续进入同一套 schema-aware connection 约束，而不是降级为 opaque。 | `visual/catalog/JavaOperatorInventoryProjector.java`、`config/GatewayConfiguration.java`、`example/InputCoercingOperatorRegistry.java`、`example/DynamicGatewayComposerService.java` |
| Resource 设计合同 | 已落地。支持 H2 registry、bootstrap、admin API、schema/secret/lifecycle 校验、stored-draft disable/delete impact guard、immutable-publication delete protection、带 changed-surface 摘要和 `ackWarnings` gate 的 draft/publication fingerprint drift warnings；validate、OpenAPI preview、warning-gated upsert 和 delete conflict 响应返回 `bloge.resourceDesignContractImpact.v1`，聚合 affected resource、`resource:<resourceId>` operatorRef、stored draft、immutable publication、draft node target、publication node target、diagnostic code counts 和 `changeRiskCounts`，让 resource-backed schema drift、disable/delete impact 与 lifecycle downgrade 能被浏览器或外部控制面机器审阅；`DEPRECATED` resource contract 在 includeDeprecated / stored draft resolution 时同样投影 `visual.operator.lifecycle.deprecated`，并进入节点级校验 warning；当 active contract 被降级为 `DEPRECATED` 且已有草稿或发布物引用时，admin validate/upsert 会返回 `visual.resourceContract.lifecycle.deprecated` 或 `visual.resourceContract.publicationLifecycleDeprecated` warning，并要求 upsert 携带 `ackWarnings=true`。 | `visual/resource/*` |
| OpenAPI resource contract 导入辅助 | 已落地。`POST /admin/resource-design-contracts/from-openapi/operations` 可先从 OpenAPI JSON/YAML 发现 path/method/operationId/tags/request-response media 摘要和基于 Preview 实际投影 response 的 READY/WARNING/BLOCKED projection readiness，浏览器可用 Discover 下拉和 readiness 摘要面板回填 selector 与审阅风险；`POST /admin/resource-design-contracts/from-openapi` 可从 OpenAPI operation 生成 `ResourceDesignContract` 草案和可审阅的 `ResourceDescriptor` 建议，支持 parsed `openApi` 或 raw JSON/YAML `openApiText` 输入、operationId 或 path+method 定位、参数/requestBody/2xx JSON response schema 投影、local component `$ref` 到 `$defs` 改写、server/path 到 `urlTemplate`、path/query/header/cookie/body 到 runtime parameter mapping，可保留 `application/*+json` 等 JSON-compatible vendor media type 到 descriptor `Accept` / `Content-Type` 并给出 review warning；`application/x-www-form-urlencoded` 和文本型 `multipart/form-data` requestBody 会投影为 `ctx.params.body`，并由 `HttpResourceOperator` 在运行时把 Map body 分别编码成标准表单请求体或带 boundary 的 multipart 请求体；binary/streaming 等仍会生成 warning 并省略 body mapping，避免伪造不可运行 descriptor；可把 HTTP bearer/basic 和 header apiKey security scheme 投影成带占位凭据的 `authStrategy` 建议；OAuth2、OpenID Connect 和 mutualTLS 会生成明确的 descriptor-suggestion warning，而不是伪造不可运行的 runtime auth；重新导入已有 resource 时返回 request/response schema、合同 metadata 和 runtime descriptor diff warnings；不会自动落库。 | `OpenApiResourceDesignContractImporter.java`、`OpenApiOperationSummary.java`、`OpenApiOperationDiscoveryResult.java`、`ResourceDesignContractAdminController.java` |
| Operator Catalog API | 已落地。支持 native、Java registry、用户导入、executable publication-backed subgraph、resource-backed virtual operators；DESIGN publication 可被查询和审阅，但不会投影成可执行 `publication:*` 子图算子；支持 search、tags、resourceOnly、includeDeprecated、tenant/namespace/environment policy filtering，以及 `sourceKind`、`loweringMode`、`capability` facet filtering（如 `design-only`、`runtime-executable`、`streaming`、`durable`、`requires-secret`、`external-effect`、`non-idempotent`）和服务端派生 `runtimeReadiness` state filtering（如 `runtime-executable`、`design-only`、`runtime-blocked`、`governance-review`、`catalog-repair-required`）；`bloge.visualOperatorCatalog.v1` 响应会返回每个 operator 的服务端派生 `runtimeReadiness` 和 `facets.total/sourceKinds/loweringModes/capabilities/runtimeReadinessStates` 计数，让大型算子目录可被控制面按可执行、设计态、运行时阻断、治理复核和 catalog repair 状态审阅；`/api/visual/operators/{operatorRef}/usage` 可返回 stored draft 和 immutable publication 对某个 operatorRef 的使用索引、保存/冻结 fingerprint 状态，并在 draft node-level operator snapshot 或 publication frozen snapshot 可用时返回 changed-surface、`changeRisk/changeCategories/changeSummary`，让已编排图能区分 breaking schema、runtime binding、governance/policy、compatible schema 和 metadata drift。 | `VisualOperatorCatalogController.java`、`VisualOperatorUsageController.java`、`OperatorUsageIndex.java`、`DefaultVisualOperatorCatalog.java` |
| GraphDraft | 已落地。支持 `bloge.visualGraphDraft.v1`、nodes、edges、inputSchema、visualLayout、output、operatorFingerprints、node-keyed `operatorSnapshots`、revisionMetadata；draft validate response 返回服务端派生的 `bloge.visualGraphReadiness.v1`，把 `valid` 的 schema/contract 结论和 `runtime-executable`、`design-only`、`runtime-blocked`、`governance-review`、`draft-repair-required` 图级 readiness 分开表达；服务端会校验 `bloge.visualLayout.v1` 的 schemaVersion、rootId、节点 operatorRef、节点/边/分组引用、分组 id 唯一性、节点 group 归属、跨分组 membership、节点/边覆盖、节点几何、edge kind / port-path metadata 和 viewport，确保 layout 仍是可验证的表现层合同。 | `visual/draft/GraphDraft.java`、`GraphDraftValidator.java`、`VisualGraphReadiness.java` |
| 草稿持久化、迁移与并发控制 | 已落地。H2 和 in-memory repository，revision history，`saveIfRevision`，controller 层 stale update/run/delete/publish guard，field-level JSON Patch；`bloge.visualGraphDraftExport.v1` 支持导出 draft snapshot、operator snapshots、export-time diagnostics 和 validation/readiness，并以新 identity 导入且刷新目标 catalog 当前 operator fingerprints/snapshots，目标 catalog 缺失时保留 bundle 中的设计时 snapshot；`bloge.visualGraphDraftImportResult.v1` 在导入后返回目标环境兼容性 diagnostics 和 validation/readiness，让 schema-only design-only、runtime-blocked 或 catalog-repair-required 图在跨环境迁移后仍保留可发布制品语义；专用 operator-fingerprint rebase API 可在用户审计 drift 后用 `expectedRevision` 显式刷新选中节点或全部节点的 service-managed fingerprint 与 operator definition snapshot，并拒绝未知节点或当前 catalog 缺失算子。 | `GraphDraftRepository.java`、`DatabaseGraphDraftRepository.java`、`GraphDraftExportBundle.java`、`GraphDraftImportResult.java`、`GraphDraftOperatorFingerprintRebaseRequest.java`、`VisualGraphDraftController.java`、`GraphDraftPatchService.java` |
| Schema-aware validation | 已落地。支持输入/输出/config schema、`oneOf`/`anyOf` union schema、contextPath/nodePath/expression/objectTemplate、data/dependency/route edges、DAG、output selection、DSL-safe path/port segments、policy、fingerprint drift、schema-only design operator draft validation、request-response runtime capability gate、non-idempotent side-effect / secret-backed execution governance warnings、runtime context validation 和 visualLayout contract validation；validate 结果会附带 `bloge.visualGraphReadiness.v1`，所以 schema-only 图可以保持 `valid=true`，但以 `readiness.executable=false` 和 `artifactKinds=["DESIGN"]` 明确进入设计制品路径；当前 resource-gateway visual runtime 会在 validate/compile/run/publish 前阻断 `streaming=true` 或 `durable=true` 的 operator，并在 compile/run/default executable publish 阶段阻断 `lowering.mode=design` 的 schema-only operator，同时保留 validate/save/export 和 `artifactKind=DESIGN` publication 的 schema 编排冻结能力；对 `NON_IDEMPOTENT` 外部副作用算子给出 production promotion 前需 review/audit 的 warning，对 `requiresSecrets=true` 算子提示 secretRef binding 和 access review；stored draft publish 在存在 warning-level validation diagnostics 且未带 `ackWarnings=true` 时返回 `409`，浏览器要求作者审阅后再次点击 Publish 才会写入 immutable publication；publication rejection 会保留 validation/readiness，让误选 `EXECUTABLE` 的 design-only 图能从响应中恢复到 `DESIGN` 发布路径。union 连接采用保守策略，source union 必须所有分支可赋值，target `anyOf` 至少一个分支可接，target `oneOf` 必须唯一分支可接；显式 `targetUnionBranch` / `targetUnionBranches` 可把 binding 和 data edge 校验收敛到用户选择的 target branch；config authoring 可通过 `visualLayout.nodes[].annotations.configUnionBranches` 选择 config schema 分支，并让 literal config、objectTemplate 和 config expression 引用按选中分支校验。 | `visual/validation/*` |
| 连接预检 | 已落地。服务端模拟 preview edge/binding/config expression，返回与当前拖拽连接相关的 schema、policy、DAG 和 DSL-safe path/port segment diagnostics，同时返回加上候选连接后的完整 candidate draft validation/readiness；这允许一次连接 accepted 但整图仍 `draft-repair-required`，浏览器可同步刷新 Server Check 和发布模式而不误杀局部拖拽。connection check request 可携带 `targetUnionBranch`，让尚未写入 draft 的拖拽预检也能按显式 union branch 消歧。 | `visual/connection/*` |
| DSL 生成 | 已落地。支持 resource virtual operator、native user operator、transform lowering、branch lowering、dependency edges、named ports、root-object bindings、structured config expression；对 schema-only design operator 给出 deterministic blocking diagnostic，而不是伪造不可执行 DSL。 | `visual/codegen/GraphDraftDslGenerator.java` |
| Compile / Run | 已落地。运行前做 fingerprint、draft validation、runtime context validation，再生成 DSL 并调用现有 dynamic runner；compile 和 transient/stored draft run 响应会回传本次 validation/readiness，publication run 响应会回传 artifact 冻结的 validation/readiness，避免客户端在 compiler、runtime context、output override 或 design-only blocking 后丢失发布/修复引导；Java operator 运行会在 gateway 边界做 typed input coercion。 | `visual/runtime/VisualGraphRunService.java`、`example/InputCoercingOperatorRegistry.java` |
| Run history / node trace 摘要 | 已落地。transient draft、stored draft 和 publication run 会生成 `runId`，并持久化来源、revision、statusMap、diagnostics、errors、elapsedMs、runtime per-node elapsedMs、DSL、context/output/results shape-only 摘要和 draft node snapshots；API 已支持 source/draft/publication/graph/outcome/limit 查询、成功率/blocked/error/latency p50/p95 stats 聚合、节点级 run count/status 分布/diagnostic attribution/selected-output count/runtime per-node latency 聚合，并保留 observed whole-run latency 供 legacy 记录和慢运行关联分析使用，以及 `/api/visual/runs/{runId}/trace` shape-only replay trace，按节点暴露 operator metadata、result summary、timingKnown/elapsedMs 和 diagnostics 归属；浏览器 Run History 面板可查看最近运行、SLO 摘要和节点热区，并打开单条运行记录和节点 trace，同时在当前画布匹配节点上显示 replay badge，并报告历史 trace 与当前画布的节点覆盖率/缺失节点。 | `visual/runtime/VisualGraphRunRecord.java`、`VisualGraphRunTrace.java`、`VisualGraphRunStats.java`、`VisualGraphRunNodeStats.java`、`DatabaseVisualGraphRunRepository.java`、`VisualGraphRunHistoryController.java`、`static/examples/gateway/app.js` |
| Immutable Publication | 已落地。发布物冻结 draft、operator snapshots、fingerprints、layout、artifactKind、DSL、validation report、generation report 和 graph readiness，并支持 H2 持久化；publication result 在成功和拒绝时保留 validation/readiness；浏览器 Server Check 面板可选择 `EXECUTABLE` 或 `DESIGN` 发布模式，并会按 readiness 的 `artifactKinds` 约束可选项；Publications 面板会展示选中 artifact 的 frozen readiness 与非执行节点行，避免用当前 catalog 重新解释历史设计制品；`EXECUTABLE` artifact 可运行并投影为 reusable subgraph operator，`DESIGN` artifact 可冻结 schema-only 编排但 run/golden UI 会禁用、run API 会返回 `visual.publication.designNotExecutable`，且不会进入 executable catalog。 | `visual/publication/*` |
| Golden regression cases | 已落地基础版。golden case 绑定不可变 publication，保存样例 context、outputNode、期望输出和可选 assertions；执行时复用 frozen publication DSL，写入 run history，并返回 exact-output、`OUTPUT_EQUALS/PATH_EQUALS/PATH_APPROX_EQUALS/PATH_EXISTS/PATH_ABSENT` value/path assertion 或 `OUTPUT_MATCHES_SCHEMA` schema assertion diagnostics。`PATH_APPROX_EQUALS` 支持绝对容差和相对容差，用于评分、金额、概率和聚合指标等非严格精确输出。API 和浏览器均支持单 case run、publication 级 suite run 汇总、latest certification，以及基于 case-set fingerprint 的 `CERTIFIED/STALE/FAILED/MISSING_CASES/UNCERTIFIED` promotion-readiness 状态。 | `visual/golden/*`、`VisualGraphRunRecord.java`、`static/examples/gateway/app.js` |
| Browser Composer | 已落地。静态页面已从 catalog 拉取 operator palette，支持服务端权威的算子库导入前 profile 与 impact review、OpenAPI/resource contract preview/save 的 `bloge.resourceDesignContractImpact.v1` 审阅和 risk-aware 二次确认，并能展示 resource contract 影响的 draft 与 publication 摘要、affected draft/publication node target chip、草稿保存/导出/导入、修订、`EXECUTABLE`/`DESIGN` 发布模式选择、schema-aware picker/connection、`oneOf`/`anyOf` union schema 本地类型提示、分支摘要可视化、root/nested target union branch 选择、config union branch 选择与 layout annotations 持久化，并可把 `visualLayout.groups` 渲染成节点背后的阶段/泳道 band；值匹配/连线兼容 advisory、Server Check 诊断按节点 label/id 聚合/过滤/聚焦/轮转，且能把 `/visualLayout/nodes/{index}` 诊断按当前 layout 顺序归因到画布节点，并在 validate/compile/run/publish/export/import 状态条显示服务端 `bloge.visualGraphReadiness.v1` 摘要，connection preflight 会消费服务端返回的 candidate draft readiness，compile/run 也会保留上一轮或服务端返回的 readiness，同时用 readiness 的 `artifactKinds` 约束发布模式，让 design-only、runtime-blocked、governance-review 和 draft-repair-required 不再只靠 diagnostics 推断；队列位置/过滤明细/隐藏节点提示，并在当前修复节点落在摘要折叠范围外时仍保留该节点预览，F8/Shift+F8 修复队列快捷导航和 Esc 清除过滤、selected-node inspector 的 validation/trace diagnostics 局部归因、connectability blocked preview 和 blocked-reason 标签、已配置节点复制和 Cmd/Ctrl+D 快捷入口、Delete/Backspace 删除选中节点并复用 impact cleanup 路径、operator usage index / fingerprint drift 查看、节点 saved-vs-current fingerprint snapshot 状态、change-risk 标签和 repair/review/rebase 动作建议、显式 rebase 动作，并把已加载的 usage 风险反馈为画布节点 badge；节点 impact inspector 可展示上下游 lineage、逐条清理 incoming/downstream input/config/dependency/route 关系，并可批量 detach 当前节点引用和 graph output 指向；palette 会展示并支持搜索 design-only、streaming/durable/suspendable/secret/effect 能力，并可按 type、tag、source、capability、runtime readiness、lowering mode 过滤大型目录，同时消费服务端 `facets` 显示 runtime-executable/design-only/runtime-blocked/governance-review/catalog-repair 以及 streaming/durable/secret/effect catalog mix，导入 profile 会优先展示 validate/import 返回的 `bloge.visualOperatorLibraryProfile.v1`，把 design-only operator 作为 info 级产品信号，把 catalog repair/runtime-blocked/governance-review 作为审阅级风险暴露；用户继续编辑 JSON 后才回退到本地即时 profile，避免过期服务端审阅误导作者。selected-node contract panel 优先消费服务端 `runtimeReadiness` 展示 runtime-executable、design-only、runtime-blocked、governance-review 或 catalog-repair 状态，缺失时才按旧 catalog metadata 本地推断；支持 DSL preview/run、run history SLO 摘要，以及 publication golden case exact-output、多条 value/path assertion、numeric tolerance assertion 和 output-schema assertion authoring、保存、单跑、suite run、certify/status，且 DESIGN publication 在 UI 中标识为非执行制品并禁用 run/golden 动作。 | `src/main/resources/static/examples/gateway/app.js`、`styles.css`、`index.html` |

## 3. 协议命名现状

早期设计文档使用了较抽象的草案名：

| 早期草案名 | 当前实现名 | 说明 |
| --- | --- | --- |
| `bloge.operatorCatalog.v1` | `bloge.visualOperator.v1` / `bloge.visualOperatorLibrary.v1` / `bloge.visualOperatorCatalog.v1` | 当前实现把单个算子、用户算子库、catalog response 拆成三个显式合同。 |
| 未单列 | `bloge.visualOperatorLibraryRevision.v1` | 当前实现新增用户算子库 create/replace/delete/restore 不可变审计快照。 |
| `bloge.graphDraft.v1` | `bloge.visualGraphDraft.v1` | 当前实现明确这是 visual authoring draft，而不是 BLOGE runtime graph AST。 |
| `bloge.resourceDesignContract.v1` | `ResourceDesignContract` record，目前 schemaVersion 由实现归一化 | 当前 API 以 resourceId 为主键管理设计时合同。 |
| `bloge.visualDiagnostic.v1` | `VisualDiagnostic` record | 当前诊断以 `level/code/message/target/line/column` 为核心字段，并可携带非敏感 `metadata` 供 impact review 等控制面做机器聚合。 |
| 未单列 | `bloge.visualGraphPublication.v1` | 当前实现新增不可变发布物协议。 |

结论：后续协议文档应以当前实现名为准，再讨论是否在平台化抽取时统一命名。不要把早期 `operatorCatalog` / `graphDraft` 草案名当成当前 API 的 wire contract。

## 4. 已完成的 Phase 1 验收

| 验收项 | 当前结论 | 证据 |
| --- | --- | --- |
| 新增带 schema 的 resource 不改前端即可进入 palette | 已满足 | catalog API + browser `loadVisualOperatorCatalog()` 动态注册 `OPERATOR_TYPES` |
| 用户可上传算子库定义 | 已满足 | `/admin/visual-operator-libraries` + operator-library revision history/restore |
| 算子 input/output/config schema 进入画布 | 已满足 | `OperatorDefinition.Ports`、`configSchema`、browser schema field rendering |
| 无 runtime 实现的算子可先做图编排 | 已满足设计制品层 | `lowering.mode=design`，validate/save/export/design publication 允许，compile/run/default executable publish 明确阻断 |
| 连线受 schema 约束 | 已满足 | `GraphDraftValidator` + `/api/visual/connections/check` |
| 草稿可保存、修订、恢复 | 已满足 | draft repository revisions + browser Drafts panel |
| 发布物不可变 | 已满足 | `VisualGraphPublication` + insert-only repository |
| 运行不依赖当前 catalog | 对 published artifact 已满足 | publication run 使用 frozen DSL |
| operator schema drift 可被阻断或预警 | 已满足 | draft fingerprint validation；library/resource replacement drift warning；publication frozen-DSL impact warning；resource contract impact review 汇总 draft/publication lineage |
| 服务端为权威校验 | 已满足 | validate/compile/run/publish 均经过服务端 validator/generator/compiler |

## 5. 仍未完成的生产级能力

| 缺口 | 影响 | 建议阶段 |
| --- | --- | --- |
| Durable / long-running graph authoring | 当前示例以 request-response 为主，已能在 catalog capability 与 draft validator 层识别/阻断 durable 或 streaming operator，但还不能覆盖长事务、人工审批、事件等待等 durable 编排。 | 平台化阶段 |
| AsyncAPI / 更广义接口导入 | OpenAPI JSON/YAML 到 resource contract 与基础 descriptor suggestion 的第一层已落地；OAuth2/OpenID/mTLS 已有明确 warning 诊断，但 token acquisition、OIDC discovery、client certificate 传输配置和完整 descriptor 自动化仍未覆盖；事件流、webhook、消息队列也仍未覆盖。 | 下一阶段 |
| Java OperatorRegistry 深化 | 已支持基础投影、typed DTO/record 运行、普通/streaming/suspendable Java source kind 可见性、suspendable -> durable capability 标记与 request-response runtime 阻断、`UnionSchema` 到 visual `oneOf` 的保守连接约束，以及 operator surface drift 摘要工具；高级泛型展示、streaming UX、suspend/resume authoring 和更丰富注解语义仍需深化。 | 下一阶段 |
| Subgraph / remote worker / AI tool source kinds | 设计已预留，当前示例覆盖 resource、native、user-library transform/branch/native lowering，以及 schema-only design authoring；remote worker / AI tool runtime binding 仍未落地。 | 后续阶段 |
| 生产 IAM / RBAC / 权限后台 | 当前 policy 是 tenant/namespace/environment availability gate，不是完整权限系统。 | 平台化阶段 |
| 多人实时协作 | 当前通过 revision guard 防止覆盖，但没有 presence、merge、operation log 或 CRDT/OT。 | 后续阶段 |
| 前端自动化回归 | 已新增 browser-facing HTTP smoke、`app.js` schema/helper probe 和 Selenium/Chrome DOM smoke，覆盖静态入口、catalog、连接预检、draft save/export/import/run/publish、publish warning 二次确认、readiness-constrained 浏览器 DESIGN publish mode、frozen publication readiness review、design publication run/golden 禁用与 catalog exclusion、draft revision preview/restore/delete、publication run、publication golden case assertion/save/run/suite run/certify/status、run history query/filter/stats UI、Java operator palette、OpenAPI JSON/YAML operation discovery、contract/descriptor preview 与 save UI wiring、用户算子库导入、operator-library replacement change-risk impact summary 和 risk-aware acknowledgement copy、palette source/capability/runtime-readiness/lowering facet filtering、catalog facets summary 和 selected-operator runtime readiness panel、palette-to-canvas 拖拽、schema-aware connection 拖拽、schema-incompatible connection rejection、`oneOf`/`anyOf` union schema 浏览器本地 hint、端口分支摘要、root/nested target branch selection 和 config union branch selection 序列化/本地消歧、visualLayout group band helper/rendering source、route/dependency/config 复杂连接拖拽、Server Check 诊断摘要/过滤/轮转快捷键/队列位置/过滤明细提示/隐藏节点提示、当前过滤节点保留可见和过滤清除、selected-node diagnostics 局部归因、connectability blocked preview / reason 标签、已配置节点复制/删除快捷入口、节点 impact lineage、incoming/downstream 关系清理和 bulk graph-output detach、无输入端口用户算子的 UI schema 投影、graph output 选择、页面 warning diagnostics 渲染、server validation failure diagnostics 渲染和 publication run。更多失败场景和更多浏览器矩阵仍需补齐。 | 下一阶段 |

## 6. 下一步优先级

1. **真实浏览器交互回归**：在现有 browser-facing HTTP + Selenium DOM smoke 基础上，补更多失败场景和更多浏览器矩阵验证。
2. **Java operator inventory 深化**：补 streaming/suspendable 画布交互、复杂泛型展示、注解扩展和 Java operator drift/兼容性策略。
3. **OpenAPI 导入深化**：继续补 binary/streaming 等非 JSON media type 的可运行编码策略，OAuth2/OpenID/mTLS 的可配置运行时接入策略和更完整的 descriptor 自动化；仍必须走现有 `ResourceDesignContractValidator`，不能让未验证 schema 进入 catalog。
4. **Run history / Golden 深化**：当前已经保存 shape-only run record，并补了查询过滤、浏览器最近运行列表、基础 SLO stats、节点级 runtime per-node latency/health 聚合、observed whole-run latency legacy/correlation 字段、带节点诊断归属和 per-node timing 的 shape-only run trace、画布 badge 和覆盖率提示、publication 级 golden case、suite run、latest certification、stale-aware promotion-readiness status、基础 golden assertions、output-schema assertions 和 numeric tolerance assertions；下一步可补事件流回放和更完整的 regression trend。
5. **文档协议收敛**：把早期草案字段名逐步改成当前实现的 wire contract，并保留平台化抽象命名作为未来 ADR，而不是混在当前协议里。

## 7. 反熵控制

为了让这个示例继续朝通用画布演进，而不是退化成一次性 demo，后续改动必须守住这些规则：

1. 新的可拖拽能力必须先进入 `OperatorDefinition`，不能只在前端 `app.js` 里硬编码。
2. 新的连接或绑定语义必须进入 `GraphDraftValidator` 和 `/api/visual/connections/check`，不能只靠浏览器判断。
3. 新的 executable draft 必须保留 operator fingerprint snapshot 和 node-level operator definition snapshot，并在 compile/run/publish 前验证。
4. 新的 resource authoring 信息必须通过 `ResourceDesignContract` 或等价合同进入 catalog projection，不能污染 runtime `ResourceDescriptor` 的调用职责。
5. 新的发布/运行能力必须返回结构化 `VisualDiagnostic`，不能只返回字符串错误。
6. README 可以描述用户体验，但协议字段和不变量必须以 `docs/bloge-visual-orchestration-implementation-status.md` 和代码为准同步更新。
