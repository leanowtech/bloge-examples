{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://leanowtech.com/schemas/resource-gateway-capability-studio/capability-studio-gate-a-class-manifest-v1.schema.json",
  "title": "Capability Studio Gate A Class Manifest v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "manifestFingerprint", "entries"],
  "properties": {"schemaVersion": {"const": "capability-studio.gate-a-class-manifest.v1"}, "manifestFingerprint": {"$ref": "#/$defs/aggregateFingerprint"}, "entries": {"type": "array", "minItems": 1, "items": {"$ref": "#/$defs/entry"}}},
  "$defs": {
    "path": {"type": "string", "pattern": "^[A-Za-z0-9][A-Za-z0-9._-]*(/[A-Za-z0-9][A-Za-z0-9._-]*)*$"},
    "fingerprint": {"type": "object", "additionalProperties": false, "required": ["kind", "algorithm", "value"], "properties": {"kind": {"enum": ["RAW_BYTES", "CANONICAL_DOCUMENT", "TREE_COMMITMENT", "AGGREGATE_COMMITMENT"]}, "algorithm": {"const": "SHA-256"}, "value": {"type": "string", "pattern": "^sha256:[0-9a-f]{64}$"}}},
    "rawFingerprint": {"allOf": [{"$ref": "#/$defs/fingerprint"}, {"properties": {"kind": {"const": "RAW_BYTES"}}}]},
    "aggregateFingerprint": {"allOf": [{"$ref": "#/$defs/fingerprint"}, {"properties": {"kind": {"const": "AGGREGATE_COMMITMENT"}}}]},
    "entry": {"type": "object", "additionalProperties": false, "required": ["binaryName", "entryPath", "rawFingerprint"], "properties": {"binaryName": {"type": "string", "pattern": "^[A-Za-z_$][A-Za-z0-9_$]*(\\.[A-Za-z_$][A-Za-z0-9_$]*)*$"}, "entryPath": {"$ref": "#/$defs/path"}, "rawFingerprint": {"$ref": "#/$defs/rawFingerprint"}}}
  },
  "$comment": "Entries are sorted by binaryName and unique. Class entries cover loadable outer production classes; non-loadable resources belong to the resource manifest. Fingerprint domain: RG-CS-GATE-A-CLASS-MANIFEST-v1."
}
