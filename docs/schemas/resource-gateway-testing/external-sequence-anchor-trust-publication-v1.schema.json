{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://leanowtech.example/schemas/resource-gateway-testing/external-sequence-anchor-trust-publication-v1.schema.json",
  "title": "Resource Gateway External Sequence Anchor Trust Publication v1",
  "$ref": "#/$defs/publication",
  "$defs": {
    "identifier": {
      "type": "string",
      "pattern": "^[A-Za-z0-9][A-Za-z0-9._:/#-]{0,254}$"
    },
    "fingerprint": {
      "type": "string",
      "pattern": "^sha256:[a-f0-9]{64}$"
    },
    "optionalFingerprint": {
      "oneOf": [
        { "const": "" },
        { "$ref": "#/$defs/fingerprint" }
      ]
    },
    "wholeSecondInstant": {
      "type": "string",
      "format": "date-time",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}Z$"
    },
    "authoritySignature": {
      "$ref": "suite-stability-serving-inventory-v1.schema.json#/$defs/authoritySignature"
    },
    "authorityKeyMaterial": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "authorityId", "keyId", "publicKeyBase64", "notBefore", "expiresAt",
        "enabled", "revoked"
      ],
      "properties": {
        "authorityId": { "$ref": "#/$defs/identifier" },
        "keyId": { "$ref": "#/$defs/identifier" },
        "publicKeyBase64": {
          "type": "string",
          "minLength": 60,
          "maxLength": 512,
          "pattern": "^[A-Za-z0-9+/]+={0,2}$"
        },
        "notBefore": { "$ref": "#/$defs/wholeSecondInstant" },
        "expiresAt": { "$ref": "#/$defs/wholeSecondInstant" },
        "enabled": { "type": "boolean" },
        "revoked": { "type": "boolean" }
      }
    },
    "material": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schemaVersion", "trustRootSetId", "sequence", "previousMaterialFingerprint",
        "scopeId", "anchorSetId", "notaryTrustDomain", "bootstrapTrustDomain",
        "receiptSignatureThreshold", "maximumFaults", "notaryKeys",
        "policyFingerprint", "issuedAt", "notBefore", "expiresAt"
      ],
      "properties": {
        "schemaVersion": {
          "const": "bloge.externalSequenceAnchorTrustMaterial.v1"
        },
        "trustRootSetId": { "$ref": "#/$defs/identifier" },
        "sequence": { "type": "integer", "minimum": 1 },
        "previousMaterialFingerprint": { "$ref": "#/$defs/optionalFingerprint" },
        "scopeId": { "$ref": "#/$defs/identifier" },
        "anchorSetId": { "$ref": "#/$defs/identifier" },
        "notaryTrustDomain": { "$ref": "#/$defs/identifier" },
        "bootstrapTrustDomain": { "$ref": "#/$defs/identifier" },
        "receiptSignatureThreshold": {
          "type": "integer",
          "minimum": 1,
          "maximum": 32
        },
        "maximumFaults": {
          "type": "integer",
          "minimum": 0,
          "maximum": 10
        },
        "notaryKeys": {
          "type": "array",
          "minItems": 1,
          "maxItems": 64,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/authorityKeyMaterial" }
        },
        "policyFingerprint": { "$ref": "#/$defs/fingerprint" },
        "issuedAt": { "$ref": "#/$defs/wholeSecondInstant" },
        "notBefore": { "$ref": "#/$defs/wholeSecondInstant" },
        "expiresAt": { "$ref": "#/$defs/wholeSecondInstant" }
      },
      "allOf": [
        {
          "if": {
            "properties": { "sequence": { "const": 1 } },
            "required": ["sequence"]
          },
          "then": {
            "properties": { "previousMaterialFingerprint": { "const": "" } }
          },
          "else": {
            "properties": {
              "previousMaterialFingerprint": { "$ref": "#/$defs/fingerprint" }
            }
          }
        }
      ]
    },
    "publication": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schemaVersion", "material", "materialFingerprint", "bootstrapSignatures"
      ],
      "properties": {
        "schemaVersion": {
          "const": "bloge.externalSequenceAnchorTrustPublication.v1"
        },
        "material": { "$ref": "#/$defs/material" },
        "materialFingerprint": { "$ref": "#/$defs/fingerprint" },
        "bootstrapSignatures": {
          "type": "array",
          "minItems": 1,
          "maxItems": 32,
          "uniqueItems": true,
          "items": { "$ref": "#/$defs/authoritySignature" }
        }
      }
    }
  }
}
