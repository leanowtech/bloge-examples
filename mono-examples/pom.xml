<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.leanowtech.bloge.examples</groupId>
    <artifactId>mono-examples</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>BLOGE Mono Examples</name>
    <description>Bloge Mono examples</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <java.version>25</java.version>
        <maven.compiler.release>25</maven.compiler.release>
        <bloge.version>0.6.5-RC2</bloge.version>
        <junit.version>5.11.4</junit.version>
        <spring-boot.version>3.4.2</spring-boot.version>
        <micrometer.version>1.14.4</micrometer.version>
        <micrometer-tracing.version>1.4.3</micrometer-tracing.version>
        <opentelemetry.version>1.46.0</opentelemetry.version>
        <maven.enforcer.version>3.5.0</maven.enforcer.version>
        <extra.enforcer.rules.version>1.8.0</extra.enforcer.rules.version>
        <maven.source.plugin.version>3.3.1</maven.source.plugin.version>
        <maven.javadoc.plugin.version>3.11.2</maven.javadoc.plugin.version>
        <maven.jar.plugin.version>3.4.2</maven.jar.plugin.version>
        <maven.checkstyle.plugin.version>3.6.0</maven.checkstyle.plugin.version>
        <checkstyle.version>13.3.0</checkstyle.version>
        <jacoco.version>0.8.14</jacoco.version>
        <spotbugs.maven.plugin.version>4.9.8.2</spotbugs.maven.plugin.version>
        <minimum.java.version>25</minimum.java.version>
        <minimum.maven.version>3.9.0</minimum.maven.version>
        <jacoco.check.skip>true</jacoco.check.skip>
        <jacoco.check.line.minimum>0.55</jacoco.check.line.minimum>
        <jacoco.check.branch.minimum>0.40</jacoco.check.branch.minimum>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- JUnit BOM -->
            <dependency>
                <groupId>org.junit</groupId>
                <artifactId>junit-bom</artifactId>
                <version>${junit.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Micrometer BOM -->
            <dependency>
                <groupId>io.micrometer</groupId>
                <artifactId>micrometer-bom</artifactId>
                <version>${micrometer.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Micrometer Tracing BOM -->
            <dependency>
                <groupId>io.micrometer</groupId>
                <artifactId>micrometer-tracing-bom</artifactId>
                <version>${micrometer-tracing.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- OpenTelemetry BOM -->
            <dependency>
                <groupId>io.opentelemetry</groupId>
                <artifactId>opentelemetry-bom</artifactId>
                <version>${opentelemetry.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Internal BLOGE dependencies -->
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-core</artifactId>
            <version>${bloge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-runtime-spi</artifactId>
            <version>${bloge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-dsl</artifactId>
            <version>${bloge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-session-ext</artifactId>
            <version>${bloge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-state-ext</artifactId>
            <version>${bloge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-agent-ext</artifactId>
            <version>${bloge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-durable</artifactId>
            <version>${bloge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-durable-mybatis</artifactId>
            <version>${bloge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-durable-codec</artifactId>
            <version>${bloge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-common-operators</artifactId>
            <version>${bloge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-spring</artifactId>
            <version>${bloge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-metrics-otel</artifactId>
            <version>${bloge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-bpmn-transformer</artifactId>
            <version>${bloge.version}</version>
        </dependency>
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-lint</artifactId>
            <version>${bloge.version}</version>
        </dependency>

        <!-- Test dependencies -->
        <dependency>
            <groupId>com.leanowtech.bloge</groupId>
            <artifactId>bloge-test</artifactId>
            <version>${bloge.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- Spring Boot -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- Micrometer -->
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-registry-prometheus</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micrometer</groupId>
            <artifactId>micrometer-tracing-bridge-otel</artifactId>
            <scope>runtime</scope>
            <exclusions>
                <exclusion>
                    <groupId>aopalliance</groupId>
                    <artifactId>aopalliance</artifactId>
                </exclusion>
            </exclusions>
        </dependency>

        <!-- OpenTelemetry -->
        <dependency>
            <groupId>io.opentelemetry</groupId>
            <artifactId>opentelemetry-exporter-otlp</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Database -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.14.0</version>
                <configuration>
                    <release>${java.version}</release>
                    <compilerArgs>
                        <arg>--enable-preview</arg>
                    </compilerArgs>
                </configuration>
                <executions>
                    <execution>
                        <id>default-compile</id>
                        <configuration>
                            <excludes>
                                <exclude>com/leanowtech/bloge/examples/integration/spring/**/*.java</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                    <execution>
                        <id>compile-spring-integration-java21</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>compile</goal>
                        </goals>
                        <configuration>
                            <release>21</release>
                            <compilerArgs combine.self="override"/>
                            <includes>
                                <include>com/leanowtech/bloge/examples/integration/spring/**/*.java</include>
                            </includes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.2</version>
                <configuration>
                    <argLine> --enable-preview</argLine>
                    <includes>
                        <include>**/*Test.java</include>
                        <include>**/*Tests.java</include>
                        <include>**/*TestCase.java</include>
                    </includes>
                    <excludes>
                        <exclude>**/*IT.java</exclude>
                        <exclude>**/*IT</exclude>
                        <exclude>**/*ITCase.java</exclude>
                        <exclude>**/benchmark/**</exclude>
                    </excludes>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <version>3.5.2</version>
                <configuration>
                    <argLine> --enable-preview</argLine>
                </configuration>
                <executions>
                    <execution>
                        <id>integration-tests</id>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                        <configuration>
                            <includes>
                                <include>**/IT*.java</include>
                                <include>**/*IT.java</include>
                                <include>**/*ITCase.java</include>
                            </includes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-source-plugin</artifactId>
                <version>${maven.source.plugin.version}</version>
                <executions>
                    <execution>
                        <id>attach-sources</id>
                        <goals>
                            <goal>jar-no-fork</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>${maven.javadoc.plugin.version}</version>
                <configuration>
                    <source>${java.version}</source>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <doclint>none</doclint>
                    <quiet>true</quiet>
                    <additionalOptions>
                        <additionalOption>--enable-preview</additionalOption>
                    </additionalOptions>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>${maven.jar.plugin.version}</version>
            </plugin>

        </plugins>
    </build>

    <profiles>
        <profile>
            <id>bloge-plugin-example</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>com.leanowtech.bloge</groupId>
                        <artifactId>bloge-maven-plugin</artifactId>
                        <version>${project.version}</version>
                        <executions>
                            <execution>
                                <id>export-example-metadata</id>
                                <phase>process-classes</phase>
                                <goals>
                                    <goal>export-metadata</goal>
                                </goals>
                                <configuration>
                                    <packages>
                                        <package>com.leanowtech.bloge.examples.integration.maven</package>
                                    </packages>
                                    <outputFile>${project.build.directory}/bloge-examples/operator-metadata.json</outputFile>
                                    <includeSpringOperators>true</includeSpringOperators>
                                </configuration>
                            </execution>
                            <execution>
                                <id>validate-example-dsl</id>
                                <phase>process-classes</phase>
                                <goals>
                                    <goal>validate</goal>
                                </goals>
                                <configuration>
                                    <sourceDirectory>${project.basedir}/src/main/resources/bloge/integration/plugin</sourceDirectory>
                                    <configFile>${project.basedir}/plugin-example.blogerc.json</configFile>
                                    <packages>
                                        <package>com.leanowtech.bloge.examples.integration.maven</package>
                                    </packages>
                                    <failOnError>true</failOnError>
                                </configuration>
                            </execution>
                            <execution>
                                <id>lint-example-dsl</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>lint</goal>
                                </goals>
                                <configuration>
                                    <sourceDirectory>${project.basedir}/src/main/resources/bloge/integration/plugin</sourceDirectory>
                                    <configFile>${project.basedir}/plugin-example.blogerc.json</configFile>
                                    <failOnError>true</failOnError>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
